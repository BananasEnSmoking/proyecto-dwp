import styled from "styled-components";

//Archivo con los colores base
import '../App.css';

export const FormCard = styled('div')`
    .form {
	padding:1.5rem;
	border-radius:10px;
	margin:1.5rem;
	background-color: var(--bg-card);
	box-shadow: 2px 2px 2px 1px rgba(0, 0, 0, 0.2);
	
    }
`;

